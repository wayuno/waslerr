# Email allowed to open the admin dashboard.
# Set this in Railway (Variables) so the build inlines it.
# Only this exact email (case-insensitive) can access /admin.
VITE_ADMIN_EMAIL=admin@waslerrfields.com
